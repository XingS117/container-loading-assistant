# 第 3 组：完整示例与真实计算阶段

沿用已授权的整体计划，当前生产为单进程 uvicorn，计算隔离进程及两个并发槽位保留，不新增数据库或队列依赖。

1. 增加 POST /api/v1/pack/jobs 和 GET /api/v1/pack/jobs/{随机任务标识}。提交后立即返回 202；后台复用同一计算服务，真实记录 AI 建议、布局计算与物理校验、结果整理、完成/失败。旧同步接口兼容。
2. 新旧入口共用并发限制。任务采用不可预测标识、no-store 响应；不在状态中保存或返回模型 Key 和原输入。最多保存 16 个任务结果、完成后 15 分钟过期；满额只淘汰已结束项。重启后明确提示任务已失效，原订单可重新提交。本期单服务进程，不承诺跨进程/重启恢复。
3. 前端默认使用短请求提交并轮询，同一次任务短暂网络失败只重试查询、不重复计算。提交阶段不盲目重试；旧服务器 404/405 才回退同步接口。总等待仍有界，超时与任务失效保留当前清单和原方案。
4. 快速开始直接载入完整 ABC 测试案例、40HQ、明确单位和假设承重/层数，用户确认后生成；替换已有清单沿用确认机制，模型设置无需配置。
5. 验证真实阶段、繁忙/异常/超时/过期/取消清理、旧接口兼容、网络恢复不重复提交、完整示例和前后端全量回归，再部署实测。
