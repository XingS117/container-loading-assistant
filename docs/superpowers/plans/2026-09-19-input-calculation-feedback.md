# 输入质量与计算反馈

沿用已批准的 9 月 13 日输入闭环方向，不更换算法和模型。

- [x] 1. Excel 显式单位识别：cm/mm/m、kg/g/t 与中文单位；精确转换、不猜无单位列；保留原始行号，一次列出错误，重复列拒绝。有错误不替换当前订单，无错误先预览再应用。
- [x] 2. 表格校验统一阻止非法提交，提示可定位具体字段；覆盖非有限数字、数量、层数、承重、卸货顺序及毫米/克精度。设置项校验不等到服务端 422。
- [x] 3. 初算/重算共用提交、等待、读取状态与耗时；计时不代表后台进度。80 秒客户端等待上限，区分 504、网络、输入、忙碌及服务响应错误；手动重试、不自动重发、不丢原订单或结果。记录初算与重算成功/失败耗时及错误类别。
- [x] 4. 测试与构建、浏览器真实导入和计算流程、推送部署、线上资源及健康验证。

边界：本轮不实现服务器任务队列/事件流，因此不能宣布原计划中的服务器内部实时分阶段进度已经完成；不擅自改变货物尺寸、不丢弃错误行、不导入部分订单。

## 本地验收

- 前端 `npm test -- --run`：101 项通过；`npm run build` 成功（保留既有大文件体积警告）。
- 后端 `.venv/Scripts/python.exe -m pytest backend/tests -q -p no:cacheprovider --basetemp=.pytest-tmp-input-feedback-0919`：192 项通过。
- 真实浏览器导入 XLSX：跨空行准确指出第 4 行的 3 项错误，不替换原订单；正确文件先预览，再将 600 mm / 18000 g 转为 60 cm / 18 kg。
- 浏览器检查 1440 px 和 390 px 宽度；手机无横向溢出，错误定位能聚焦对应输入。
- 模拟 HTML 504 验证提示与清单保留，解除模拟后手动重试真实接口返回 200、三方案各装 10 件（请求 `951e39fb9847`）。
- 回归覆盖初算失败后重试、重算失败保留原方案并记录耗时/类别、计时器清理、80 秒中止、422/429/繁忙分类和不自动重发。
- 浏览器的 favicon 404 为既有图标缺失；504 为本次明确注入的失败场景。

## 生产验收

- 代码版本 `f0d45c3`，已推送 GitHub main 并部署到 `https://packing.xingshuwen.com`。
- 服务 active，公网首页 200、`/health` 返回 ok。
- 主资源 `index-B4UteYxL.js`；公网与本地 SHA-256 一致：`08A6E14F8A31E1615C7AB2C3C7CAB29ACF39D70428331DBCE01B0ECF33113F14`。
- 公网浏览器重复通过真实 XLSX 导入、批量错误、单位转换、定位、390 px 手机无溢出测试。
- 显式模拟 HTML 504 后订单不丢失，解除模拟后真实初算请求 `53eba8db4b8d` 返回三方案、各 10 件。
- 显式模拟重算断网后保留原结果；解除模拟，换 40HQ 真实重算请求 `3c837e9a7a58` 返回三方案、各 10 件。
- 生产 Umami tracker 已加载；耗时与错误类别通过前端测试验证，未访问统计后台确认入库报表。
- 回滚包：`/data/packing-assistant/backups/before-f0d45c3.tar.gz`。
- 本轮剩余 0 / 4。9 月 13 日整体计划仍未全部验收；本轮不宣称消除后台 504、不宣称完成算法内部实时进度。
- 下一步：人工编辑移出货物转未装清单、交换位置与安全复核，再补结果历史和真实订单评测闭环。
