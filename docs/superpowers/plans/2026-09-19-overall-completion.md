# 9 月 13 日整体计划剩余执行清单

目标仍是原计划全部执行、推送、部署并完成逐项验收，不用单轮完成替代整体完成。上一轮有实质进展：已提交离线评测基线与工具，当前 HEAD d3a050f，生产代码 21116cd。

已交付项（以各自验收记录及当前代码为依据）：Excel/输入质量、错误分类与请求阶段/耗时、人工调整安全与吸附、移出/交换、锁定重算、作业单与签名、完整结果历史、离线可复现评测。仍须在最终整体验收中复核。

- [ ] 1. 目标与质量解释：每方案求解状态、目标/硬约束/软目标、相对高装载率的量化取舍；大空白区域数量/面积、直接支撑、连续性、碎片与 SKU 切换、估算搬运距离；结果与打印一致，风险可定位到区域或货物，编辑后重算解释。
- [ ] 2. 布局目标优化：增加安全有界上层聚拢候选；stable 抑制上层大空白且控制重心，easy 比较连续区域、步骤和距离，保留少装披露；新旧版本评测与全量回归。
- [ ] 3. 快速开始与计算阶段：新用户可一键载入完整示例；真实后台阶段可查询，长计算不依赖单一 HTTP 等待；错误恢复和旧接口兼容。
- [ ] 4. 可统计闭环：输入完成、计算/超时/导出/调整/反馈的关联与口径；真实浏览器检查上报，不发送订单明细和密钥；明确统计后台/实际改善证据。
- [ ] 5. 真实订单评测与后续约束：利用可追溯用户案例补充回归，区分实际发货/测试输入/人工认可；记录多柜、成本、通道、固定与包装强度的证据、优先级和未具备条件。至少一组真实订单与现场认可、手动调整下降仍需实际证据，不可伪造。
- [ ] 6. 整体发布验收：对照原计划每条款、接口/浏览器/手机/打印/生产实测、全量测试构建、备份部署、GitHub 推送；所有强制验收有证据后才标记目标完成。

第 1 组设计：新增兼容性可选 `assessment`，与核心 metrics 分离以兼容旧历史。几何分析使用同高度投影，矩形并集不重复计数；空白含凹口并非均为封闭洞。大空白定义面积至少 0.25 平方米，规则展示给用户；直接支撑与空间连续性分开。估算距离为每件从柜门至纵向位置的往返水平距离，不等于实测工时。复杂分析受限时返回明确“不完整”而非伪造零值。人工编辑经后端复核后重建当前方案解释和比较，不沿用旧承诺。

数据证据更新：原任务 `019ffbef-798a-7d93-ab72-530a7cec650d` 的用户消息明确提供 A(650/650/1200 mm,150kg,30)、B(890/750/1120 mm,280kg,30)、C(1080/800/1250 mm,400kg,3)。这可证明为用户提供测试输入，但仍不证明现场发货、承重约束或满意结果。
