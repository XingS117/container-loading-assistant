# 完整示例与真实任务阶段验收

新增完整 ABC 测试示例入口，明确 40HQ、63 托、单重及承重/层数假设；替换清单前保留确认，不配置模型也可计算。新界面通过短请求提交任务和查询阶段，旧 POST /api/v1/pack 保留。任务与同步计算共享两个名额，安全规则与计算预算未放宽。

任务只驻留单 API 服务进程，随机任务编号可查询结果，请勿转发编号。最多 16 个任务，结束后 15 分钟过期，满额可提前淘汰最旧已结束项；无运行任务被淘汰。服务重启后需重新提交。返回 no-store，不写订单或 API Key 到任务数据库/磁盘。未来启用多 API worker 前必须替换为共享任务存储，本次未做此部署变更。

验证：

- 后端 218 passed；前端 126 passed；TypeScript/Vite 构建通过。
- 异步接口提交立即返回 202、真实 solving 阶段、结果三方案、新旧接口共享忙碌限制、错误分类、过期清理与启动前取消释放槽位均有测试。
- 前端验证轮询只发送随机任务编号、不重发 AI Key；短暂查询失败只重查同一任务；仅在旧服务器 404/405 时回退旧接口。
- 本地浏览器 ABC 示例成功完成，三方案各 63 件，手机无横向溢出。真实重算时故意中断一次查询，任务提交仍只有 1 次，166ms 收到接单响应，约 2.47s 恢复并显示结果。观察到 ai、complete 阶段；快速案例可能在相邻轮询间完成 solving，页面不伪造未捕获的中间进度。
- 控制台的查询连接错误由上述故障注入产生；除此只有开发 favicon 404。截图位于 ignored output/playwright/job-*.png。

短请求解决代理等待链路，并不保证任意复杂订单总能求得最优。算法仍有预算内安全备选、明确失败和原方案保留机制。
