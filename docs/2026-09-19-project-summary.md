# 装柜系统升级项目总结

日期：2026-09-19。线上地址：https://packing.xingshuwen.com/

## 一、交付结论

9 月 13 日升级计划的工程交付已上线，并完成回归、生产操作验证及 GitHub 推送。根据用户最新指示“现在没有真实的订单，先用原有历史案例做订单测试，测试通过后写项目总结”，本次以历史 ABC 订单生产测试作为收尾验收，结论为通过。

这不是现场装柜认证。真实发货、包装强度、仓库装卸认可和满意度长期趋势，改为有实际订单后的观察事项，不再作为本次项目收尾的阻塞条件。

## 二、完成内容

| 工作方向 | 已交付能力 |
| --- | --- |
| 输入与计算 | 完整示例、Excel 数据质量检查、字段定位、三目标提交；异步任务接单及真实阶段查询，分类显示输入、网络、繁忙和超时错误 |
| 布局与解释 | 本地物理校验为最终裁决；三方案目标区分、安全有界候选优化、空白/支撑/重心/连续性/操作指标、取舍与风险定位 |
| 人工调整 | 位置与允许朝向调整、吸附、合法落位、旋转、交换、移出、撤销、锁定重算；不能改变货物尺寸与重量 |
| 结果交付 | 本机订单与独立方案历史、复制清单、未装货物记录、逐件作业单、三方案打印及签名位 |
| 验证与反馈 | 六案例离线评测、版本比较；计算、切换、调整、打印和反馈事件关联，隐私过滤及统计异常隔离 |

## 三、本次历史订单实测

使用用户此前提供的 ABC 数据，在全新浏览器中访问生产网站，通过“载入完整示例”录入并实际计算，未使用模拟接口。

| 货物 | 尺寸（mm） | 单重 | 数量 |
| --- | --- | --- | --- |
| A / ZT1 | 650 × 650 × 1200 | 150 kg | 30 托 |
| B / ZT2 | 890 × 750 × 1120 | 280 kg | 30 托 |
| C / ZT3 | 1080 × 800 × 1250 | 400 kg | 3 托 |

使用 40HQ，合计 63 托、14.1 吨。零间隙、正放转向、A/B 最多两层、C 单层、顶部承重 500 kg、柜门目标预留 300 mm 沿用测试设定，未经厂家或现场认证。脚本逐项比对了历史尺寸、重量、数量、朝向与叠放规则，没有更改货物属性来提高装入量。

2026-09-19 18:45（北京时间）生产测试，接口 81 ms 返回 HTTP 202，页面约 3.696 秒显示结果。本次没有出现 422 或 504，不代表任意复杂订单都不会超时。

| 指标 | 装载率优先 | 重心稳妥 | 易操作 |
| --- | --- | --- | --- |
| 装入 / 订单 | 63 / 63 | 63 / 63 | 63 / 63 |
| 体积利用率 | 53.54% | 53.54% | 53.54% |
| 前后 / 左右重心偏差 | 6.51% / 11.72% | 2.36% / 0.96% | 25.33% / 10.04% |
| 上层最小直接支撑率 | 100% | 100% | 100% |
| 上层最大同高平面空白 | 0 m² | 0 m² | 0 m² |
| 底层包围范围内空白 | 3.243 m² | 3.243 m² | 1.983 m² |
| 装载步骤 | 2 | 22 | 3 |
| 实际柜门预留 | 392 mm | 392 mm | 2732 mm |
| 独立物理与数量复核 | 通过 | 通过 | 通过 |

三份线上结果均经本地权威校验器独立复核，覆盖边界、碰撞、原尺寸/重量、允许朝向、完整支撑、承重、叠放层数、件数及步骤一致性。本次推荐“重心稳妥”：保持全部件数且重心偏差较小，但步骤更多。“易操作”同类集中、SKU 切换较少，重心和估算搬运距离仍有代价，不能称为所有指标最优。

空白指标按同高度货物平面包围范围计算，不包含整个柜内剩余空间；上层空白为零不等于整柜无空隙，更不等于运输动态安全。侧向固定、填隙与包装强度仍需现场核实。

本次 AI 状态为 `considered`、`applied=false`：已取得建议，但未形成被采用的安全引导候选，最终使用本地安全算法。没有将本次布局质量归功于 AI，也未更换模型或密钥。线上含 AI 调用/预算的运行与离线确定性评测不是同一次试验，“易操作”结果不同，因此本表不与旧离线结果冒充严格性能对比。

## 四、验证与版本

- 本次历史案例：三方案切换成功，刷新后完整方案不变；三份打印作业单各 63 行；390px 手机无横向溢出；桌面/手机 3D 截图人工查看正常；控制台 0 错误、0 警告，公共健康接口正常。
- 本轮此前全量回归：后端 219 项、前端 130 项及 TypeScript/Vite 构建通过；六案例重复生成的 36 份布局合法可重复。没有把此前测试称为本次重新执行。
- 此前生产完整操作链：移出单件、锁定剩余货物、应用、刷新、打印和重算通过，锁定件位置、朝向、尺寸与重量保持不变。
- 生产应用代码为 `9c370e9`；此前发布验收文档提交为 `e770e2f`。本次只增加测试证据与总结，不修改应用，不需要重复重启生产服务。
- 当前 JS 为 `index-C4TulfCu.js`，SHA256：`8a5124f6f45b2ea897edac3cbcf29f67da63686b0c6d6f0d76c063f6bc87973e`。

证据：[本次历史订单测试](evaluations/2026-09-19-historical-order-acceptance.json)、[逐条工程验收](audits/2026-09-19-september13-final-audit.md)、[六案例最终对比](evaluations/2026-09-19-six-case-final-comparison.md)。

## 五、交付边界与后续

本次约定范围内剩余任务为 0。历史案例验收通过不表示算法达到全局最优，也不表示已证明真实用户满意度持续提高。

锁定重算是保留锁定堆叠、利用剩余空闲底面的有界求解，不是任意三维局部重优化；异步任务为单服务进程短期内存任务，服务重启后需重新提交；订单历史仍存本机。现有构建大包告警、长期性能趋势、叉车路径、包装强度和绑扎等约束属于后续工作，不能冒充已经验证的能力。

有真实订单后，优先复核实际承重/叠放规则，再记录仓库调整量、可操作性及满意度；用现场数据决定下一轮优化，不再为本次交付追加未经验证的功能。
